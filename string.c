#include<stdio.h>
#include<string.h>
struct id_card{
    char name[5];
    int roll_num;
    int class;
};

void main(){
    struct id_card;
    struct id_card;
    printf("enter name");
    gets(shubhi.name);

    shubhi.name="shubhi";
    shubhi.roll_num=25;
    shubhi.class=12;

    printf("name=%s\n roll=%d\n class=%d\n", shubhi.name= shubhi.roll_num=25 shubhi.class);
}